TOTALS: {'found': 45, 'valid': 17, 'partial': 28, 'dismissed': 0}

======================================================================
① 초심자 이해도 — 발견 9건
VERDICT: 초심자는 Core의 큰 그림(반복→독립 검증→정지, 자기보고 금지)에는 도달하지만, 스펙이 정의 없이 쓰는 자체 용어들(Δ, gene, pass-threshold, structure, '분리된 컨텍스트'의 충족 조건, '산출물 변화 없음')에서 체계적으로 걸려 넘어진다 — 독자의 confusions 7건 중 5건이 이 미정의 식별자/기준 문제로 수렴한다. 특히 Δ와 gene은 Core의 MUST 로그 스키마에 박혀 있어 "Core만 가볍게 시도"하려는 바로 그 진입 경로를 막고, §7은 개념 4개를 종합 도식 없이 투하해 선언된 진

  [major→minor|partially-valid] Core 필수 로그 필드 `delta`(Δ, "방향 전환")가 스펙 어디에도 정의되지 않았다
    section: §4.2.8
    desc: §4.2.8은 로그 항목에 `delta`(방향 전환 시)를 MUST로 요구하고, §5.3은 "직전 상태(Δ 포함)"를 구현 컨텍스트에 제공하라 하며, 부록 A 최소 예시의 운영 지침 1번마저 "직전 시도의 Δ를 확인한다"이다. 그런데 '방향 전환'이 무엇인지(전략 변경? 검증 점수 반전? 접근법 폐기?)는 본문 어디에도, 부록 C 용어집에도 없다. 순진한 독자의 minimal-loop 답변이 Δ를 아예 언급하지 못한 것이 증거다 — Core만 쓰는 첫 구현자가 
    fix: 부록 C에 'Δ(방향 전환)' 항목을 추가하고 §4.2.8에 한 줄 정의를 넣는다. 예: "delta: 직전 iteration과 다른 접근을 택한 경우, 폐기한 접근과 새 접근을 요약한 문자열". 부록 A 로그 예시 1-2줄을 함께 제시.
    판정근거: 사실 관계는 맞다. §4.2.8이 `delta`(방향 전환 시)를 MUST 로그 필드로 요구하고, §5.3·부록 A가 Δ를 기지(旣知) 개념처럼 참조하지만, 부록 C 용어집을 포함해 스펙 어디에도 '방향 전환'의 조작적 정의가 없다 — 자기완결성 관점의 실제 결함이다. 다만 major는 과장이다. §4.2.8에 '(방향 

  [major→major|partially-valid] '유전자' 개념이 정의 없이 Core를 관통하고, Core 전용 루프의 로그에까지 `gene` 필드가 MUST다
    section: §4.2.8
    desc: 독자가 명시적으로 혼란을 보고했다: "§7.1을 읽기 전까지 '유전자'가 LOOP.md의 특정 버전인 줄 몰랐고, 정의는 맨 뒤 부록 C에 나온다." 실제로 용어는 §1.2 원칙 5, §2 디렉터리 주석, §4.2.8에서 정의 없이 먼저 등장한다. 더 나쁜 것은 §4.2.8이 Evolution을 켜지 않은 Core 루프에도 `gene`(유전자 버전 식별자)을 필수 로그 필드로 요구한다는 점이다 — Core(§1–6)만으로 완결적 표준이라는 §7 서두의 약속과 달
    fix: ① §1.2 원칙 5 첫 등장 시 괄호 정의 추가: "유전자(gene, 특정 버전의 LOOP.md 내용)". ② §4.2.8에서 `gene`을 선택 필드로 낮추거나, 비진화 루프의 기본값을 명시한다(예: "Evolution 미사용 시 고정값 `v1` 또는 LOOP.md 해시").
    판정근거: 두 주장 중 첫째('정의 없이 먼저 등장')는 부정확하다 — §1.2에 인라인 글로스, 부록 C에 정식 정의가 있으므로 '정의 없이 관통'은 과장이다. 그러나 둘째 주장은 실질 결함이고 major가 맞다: §4.2.8은 `gene`을 모든 로그의 MUST로 규정하고 §6 Level 1(Runner, 비진화)도 append

  [major→minor|partially-valid] stagnation의 '산출물 변화 없음' 판정이 전부 구현 정의라 종료 상태가 런타임마다 달라진다
    section: §4.2.4
    desc: 독자가 "이 필드가 실전에서 얼마나 신뢰 가능한 신호인지 감이 안 왔다"고 보고했다. §4.2.4는 점수 없을 때 '산출물 변화 없음'으로 무진전을 판정하라면서 "세부는 구현 정의"로 넘긴다. '산출물'의 범위(작업 디렉터리 전체? verify가 참조하는 파일만?)와 '변화 없음'의 기준(바이트 동일? 해시?)이 없으므로, 동일한 LOOP.md가 런타임 A에서는 `stalled`, B에서는 `max-iterations`로 끝난다 — 종료 상태 6종을 MUST로 
    fix: 최소 기계 기준 하나를 규범으로 제시한다: "점수 부재 시 무진전의 기본 정의는 '해당 iteration의 verify 결과(통과/실패 및 출력)가 직전과 동일하고 산출물 파일 해시 집합에 변화가 없음'이다. 이를 벗어나는 판정을 쓰는 런타임은 자기 기준을 문서화해야 한다(MUST)".
    판정근거: 무진전 판정이 런타임마다 달라져 동일 LOOP.md의 종료 상태가 stalled↔max-iterations로 갈릴 수 있다는 지적 자체는 사실이고 재현성 관점의 실제 틈이다. 그러나 major·'정면 충돌'은 과장이다. ① 스펙이 '세부는 구현 정의'로 의도적으로 스코프한 사항이고, ② §4.2.4의 종료 상태 6종 MU

  [major→minor|partially-valid] verify.agent의 '분리된 컨텍스트'가 조작적으로 정의되지 않아 Level 1 준수 여부를 판정할 수 없다
    section: §4.2.5
    desc: 독자가 "별도 API 호출인지 별도 세션인지, 런타임이 격리를 실제로 어떻게 보장하는지 상상이 안 됐다"고 보고했다. §4.2.5는 '대화 이력을 공유하지 않는 신선한 컨텍스트'(MUST)라고만 하는데, 이것이 프롬프트 수준 분리(같은 세션에서 '너는 이제 비평가다')로도 충족되는지가 불명확하다. 대조적으로 §7.2 홀드아웃은 "프롬프트 지시만으로는 준수가 아니다"를 명시한다 — 이 비대칭 때문에 독자는 verify 분리는 프롬프트로 때워도 된다고 역추론할 수 
    fix: §4.2.5에 충족 조건을 명시: "분리된 컨텍스트란 구현 컨텍스트의 대화 이력·중간 산출 요약·자체 평가가 입력에 포함되지 않는 새 세션/호출을 뜻한다. 비평가 입력은 산출물과 verify 지시문 파일로 한정한다(SHOULD). 프롬프트 역할 전환만으로는 준수가 아니다(§7.2와 동일 원칙)."
    판정근거: 지적이 스펙의 기존 문면을 과소평가한다: '대화 이력을 공유하지 않는 신선한 컨텍스트(MUST)'는 같은 세션 내 역할 전환('너는 이제 비평가다')을 이미 배제한다 — 같은 세션은 이력을 공유하기 때문. 따라서 '프롬프트로 때워도 반박 근거가 없다'는 주장은 부정확하다. 다만 결함이 전무한 것은 아니다: 신선한 컨텍스트

  [major→major|valid] ratchet/gated 대상의 주소 체계가 없어 부록 B의 `pass-threshold`·`structure`가 무엇을 가리키는지 알 수 없다
    section: §7.3
    desc: 독자가 부록 B의 `ratchet: [{ pass-threshold: higher-is-stricter }]`에 대해 "정확히 무엇의 pass-threshold인지(verify 중 어느 것?) 모호했다"고 보고했다. `pass-threshold`는 스펙이 정의한 frontmatter 필드가 아니고(§4.1 표에 없음), 부록 B의 verify 3개 중 어느 것의 임계값인지 지시할 문법도 없다. `gated: [stop, structure]`의 `structure
    fix: §7.3에 대상 지정 문법을 규정한다: frontmatter는 점 표기 키 경로(예: `verify[2].consensus`, `stop.stagnation`), 본문은 마커 이름. 예약 라벨(`structure` 등)을 쓰려면 그 의미를 표로 정의하고, 부록 B 예시를 실존 필드 경로로 교체한다.
    판정근거: 실제 결함이다. §7.3은 free/ratchet/gated를 '대상'의 string[]로만 규정할 뿐 대상 지정 문법(키 경로·마커 이름·라벨)을 정의하지 않는다. 부록 B의 `pass-threshold`는 §4.1 어디에도 없는 필드이고 verify 3개 중 무엇의 임계값인지 지시할 수 없으며(임계값은 goal 문자열

  [minor→minor|valid] budget.per-iteration의 표 서술이 '예산 초과 방지 필드'로 오독을 유도한다
    section: §4.2.6
    desc: 독자가 "그냥 예산 초과 방지용인 줄 알았다가 뒤늦게 재해석해야 했다"고 보고했다. 원인은 서술 순서다: §4.2.6 표의 의미란이 'iteration당 자원 상한'이라 다른 두 상한(max-cost-usd, max-duration)과 같은 부류로 읽히고, 진짜 목적(비교 가능성)은 표 아래 세 번째 문단에야 나온다. 필드가 `budget` 아래에 있다는 위치 자체도 오독을 강화한다.
    fix: 표 의미란을 "iteration당 동일 자원 조건 — 목적은 총액 통제가 아니라 iteration 간 점수 비교 가능성(§4.2.4)"으로 고치고, 목적 문단을 표 바로 다음 첫 문장으로 올린다.
    판정근거: 구조적 지적이 정확하다. §4.2.6 표 의미란은 'iteration당 자원 상한'이라 max-cost-usd·max-duration과 같은 '총량 상한' 부류로 읽히고, 진짜 목적('총액 통제가 아니라 iteration 간 비교 가능성')은 표 아래 별도 문단에야 나오며 필드가 `budget` 하위에 위치한 점이 오독을

  [minor→minor|valid] Level 0 요구사항 '없음'이 바로 뒤 문장과 자기모순이라 강제 시작점을 두 번 읽어야 파악된다
    section: §6
    desc: 독자가 "'요구사항 없음'이 이상했고, 스펙이 강제하는 게 어디부터인지 표를 두 번 읽어야 확실해졌다"고 보고했다. §6 표의 Level 0 행이 '없음'이라 해놓고 같은 칸에서 'MUST는 파일 형식에만 적용'이라고 하는데, 파일 형식 MUST(§3–4의 name 규칙, frontmatter 유효성 등)가 적용된다면 요구사항이 없는 게 아니다.
    fix: 요구사항 칸을 "파일 형식 준수(§3–§4)만. 런타임 행위 요구 없음 — LOOP.md는 에이전트가 세션 안에서 읽고 따르는 문서로 기능"으로 교체해 '없음'이라는 단어를 제거한다.
    판정근거: 실제 내부 긴장이다. §6 표 Level 0 칸이 '없음'이라 해놓고 같은 칸에서 '스펙의 MUST는 파일 형식에만 적용된다'고 한다 — 파일 형식 MUST(§3–4의 name 규칙·frontmatter 유효성)가 적용된다면 '요구사항 없음'은 정확한 서술이 아니다. '없음'을 '파일 형식 준수만, 런타임 행위 요구 없음

  [minor→minor|partially-valid] §7이 신개념 4개(홀드아웃·변이표면·게이트·승격 사다리)를 도식 없이 연속 투하해 상호 관계 학습에 실패한다
    section: §7
    desc: 독자가 "각 개념이 어떻게 맞물리는지, 예컨대 ratchet과 게이트의 경계가 어디인지 텍스트만으로는 한 번에 소화하기 어려웠다"고 보고했다. §7.1의 한 줄 파이프라인은 [자동 적용 | 래칫 | 인간 게이트]를 병렬 나열할 뿐, 어떤 변경이 어느 경로로 가고 어느 단계에서 홀드아웃 비교가 필수인지의 분기 논리는 §7.2–7.4에 흩어져 있다(래칫 완화→gated는 §7.3 마지막 항목에 한 줄). 스펙 전체에서 개념 밀도가 가장 높은 절인데 유일하게 종합 표
    fix: §7.1 직후에 변이 경로 결정표를 추가: 행=변경 유형(free 필드/ratchet 강화/ratchet 완화/gated 필드/본문 FREE 블록/goal), 열=자동 적용 가능 여부·인간 승인·홀드아웃 비교 필수 여부·근거 절 번호.
    판정근거: '유일하게 종합 표가 없는 절' '도식 없이 연속 투하'는 과장이다 — §7의 각 하위 절은 이미 개별 표를 갖는다. 다만 지적의 핵심(변경 유형 → 자동/래칫/게이트 경로 + 홀드아웃 비교 필수 여부를 한눈에 보이는 '경로 결정표'의 부재, 그리고 '래칫 완화→gated'가 §7.3 마지막 한 줄에 흩어진 점)은 유효한

  [minor→minor|partially-valid] Core만 읽어도 신개념 7-8개를 요구하는데 최소 예시가 부록 A까지 지연된다
    section: §1
    desc: SKILL.md는 '지식을 파일로' 개념 1개로 잡히는 반면, 이 스펙의 Core는 독립 검증 루프·자기보고 금지·유전자/인스턴스 분리·Δ·green-runs/stagnation·per-iteration 공정성·준수 수준·append-only 로그까지 7-8개 신개념을 동시에 도입한다. 독자는 결국 올바른 최소형에 도달했지만 "stop이 max-iterations나 max-duration 하나만 있어도 된다는 걸 부록 A를 보고서야 확신했다"고 보고했다 — 규범
    fix: §1 끝(또는 §3)에 부록 A의 5필드 예시를 10줄 이내로 인라인하고 "이것이 유효한 최소 루프의 전부다"를 명시한다. §1 첫 문장의 '독립 검증'에 §1.2 원칙 2로의 앞참조를 달아 차별점을 30초 구간에 고정한다.
    판정근거: 최소형의 규범적 근거는 이미 명확히 서술돼 있다 — 필수 5필드는 §1.2·§4.1에, stop의 최소 요건은 §4.2.4 MUST에 못박혀 있다. 따라서 '규범 절만으로 최소형을 확신하지 못하고 부록 A로 역산했다'는 전제는 스펙 문면과 어긋나 과장이다. 다만 개념 밀도가 높은 것은 사실이고, §1이나 §3에 10줄짜리

======================================================================
② 대체재 시험 — 발견 7건
VERDICT: 대체재 6종 모두에서 스펙의 잔여가치(정지 상한 MUST·자기보고 금지·append-only·홀드아웃·래칫·배포 규격)가 실재하므로 완전 공백 blocker는 없다. 그러나 스펙 문서 자체는 어떤 대체재에 대해서도 '왜 X가 아닌가'를 한 문장도 답하지 못한다 — 답변 재료는 전부 research/wiki에 산재하고 스펙의 유일한 비교 대상은 SKILL.md(§1.1)뿐이며, 가장 강한 압박(Ouroboros Seed)은 차별 축이 선택 확장 §7에 몰린 Core의 얇은 잔여를 정확히 찌른다. 비규범 포지셔닝 부록 하나와 플레이북 

  [major→major|valid] [횡단·전체 X] 스펙에 선행물·대체재 포지셔닝 절이 전무 — 6개 대체재 모두에 대해 스펙 텍스트 단독으로는 '왜 X가 아닌가'에 한 문장도 답하지 못함
    section: missing
    desc: 스펙이 비교하는 대상은 SKILL.md(§1.1) 하나뿐이다. Ralph/PROMPT.md 관행, Anthropic ralph-wiggum 플러그인, Ouroboros Seed, 워크플로 엔진, 동명 선행물 sdwolf4103/loop.md, ACE/DC 메모리 라이브러리 중 어느 것도 언급이 없다. 답변 재료(정지 상한 MUST §4.2.4, 자기보고 금지 §4.2.5, 홀드아웃 §7.2, 래칫 §7.3)는 스펙 안에 있으나 대체재와 연결하는 문장이 없고, 차
    fix: 비규범 부록 'Prior Art와 포지셔닝' 신설: 대체재 6종 × (그것이 제공하는 것 / 스펙이 더하는 것 / 해당 절) 매핑표. research §5.1의 실존 필드 조사표를 스펙으로 이관하면 된다.
    판정근거: 사실 정확. 스펙이 명시적으로 비교하는 대상은 §1.1의 SKILL.md 하나뿐이다. Ralph/PROMPT.md·ralph-wiggum·Ouroboros Seed·워크플로 엔진·동명 sdwolf4103/loop.md·ACE/DC 중 어느 것도 스펙 본문에 언급이 없고, 대체재와 스펙의 잔여가치를 연결하는 문장이 전무하다

  [major→minor|partially-valid] [X2 Ouroboros Seed] Core(§1–6) 단독의 대체 잔여가치가 얇음 — 차별 축이 전부 선택 확장 §7/Level 2에 몰려 있어 Seed가 필드 몇 개만 추가하면
    section: §7
    desc: Seed는 goal/acceptance_criteria/exit_conditions/constraints(생성 후 불변)/정체 감지/세대 하드캡/3단 비용 계층 검증을 이미 갖췄고 작동하는 런타임까지 있다(research/loop-md-round2-deep-dive.md §5.1). 스펙의 선점 논거였던 '홀드아웃 격리+자기보고 금지+래칫의 선언 파일 레벨 형식화'(round2 §5.1 공백 확인) 중 홀드아웃·래칫·게이트는 전부 선택 확장 §7이고 Level 
    fix: §1 또는 §1.2에 'Core의 비타협 3규칙(정지 상한·자기보고 금지·append-only)은 필수이며 기존 어떤 루프 선언 포맷에도 필수로 존재하지 않는다'를 명시하고, 부록에 Seed↔LOOP.md 필드 매핑표(마이그레이션 경로 포함)를 추가.
    판정근거: 부분적으로만 타당. '차별 축이 전부 §7/Level 2에 몰려 있다'는 프레이밍은 과장이다 — 자기보고 금지(§4.2.5 MUST NOT)와 append-only(§4.2.7 MUST NOT)는 Core이자 Level 1 강제 대상이다(§6). 발견 스스로도 이 3규칙이 Core 잔여임을 인정하므로 내부 모순이 있다. 

  [major→major|valid] [X6 ACE/DC] 플레이북 직렬화 포맷 미정의 — '라이브러리 대신 파일 표준'의 유일한 논거인 이식성이 스펙 안에서 성립하지 않음
    section: §7.5
    desc: ACE 옹호자의 최강 논변: '자기개선 메모리는 라이브러리가 해결했고(증분 델타 갱신으로 context collapse 회피), DC의 결정론 재실행 10%→99%는 스펙 §7.5가 직접 인용하는 우리 결과다. 파일 표준 없이 아무 에이전트에 끼우면 된다.' 이에 대한 유일한 반론은 크로스-런타임 이식성인데, §7.5는 지위 3단(휴리스틱/규칙/공식)과 강등 MUST만 규정할 뿐 플레이북 항목의 직렬화 형식·지위 표기 방법·k회 유효 카운트 기록 방식을 전혀 정
    fix: §7.5에 플레이북 항목 최소 스키마(지위, 진입 근거 로그 참조, k 카운트, 전제조건) 정의. 최소한 §8.1 배포 대상인 playbook/generic/의 형식 요건만이라도 규범화.
    판정근거: 정확하고 실질적. §7.5는 지위 3단(휴리스틱/규칙/공식)과 강등·격리 MUST만 규정하고, 플레이북 항목의 직렬화 형식·지위 표기·k 카운트 기록 방식을 어디에도 정의하지 않는다. §2 디렉터리 구조도 playbook/generic/·local/ 존재만 표시할 뿐 파일 포맷 무언급. 그런데 §8.1은 playbook/

  [major→minor|partially-valid] [X5 sdwolf4103/loop.md] 동명 선행물이 실재하는데 파일 내 스펙 식별자·버전 필드가 없어 런타임이 어떤 'LOOP.md 방언'인지 판별 불가
    section: §4.1
    desc: sdwolf4103/loop.md('Markdown-native while loop for AI agents', 2026-06-16)는 정확히 같은 파일명·같은 목적으로 선행하며 프로젝트 자체 조사가 이를 확인했다(research/agent-skills-and-loop-md.md:178 — '선점 실사와 차별화 서사가 필요'). 그러나 스펙은 이 충돌을 언급하지 않고, §4.1 frontmatter에 스펙 식별/버전 필드가 없으며 §10은 명세 문서의 버전만 다
    fix: 선택 필드 `spec`(예: spec: agent-loops/0.2)을 §4.1에 예약하고, 부록에 동명 선행물과의 구별을 한 줄 명시. 메이저 버전 파일 판별 규칙을 §10에 추가.
    판정근거: 결함은 실재하나 두 갈래 중 한쪽만 강하다. §4.1 필드표에 spec/spec-version 식별 필드가 없고 §10은 명세 문서 버전만 다루는 것은 사실. (a) 동명 선행물 판별 불가 주장은 약하다 — goal/stop/verify의 구조화된 필수 구조 자체가 sdwolf식 while-loop 파일과 구별되는 지문 

  [major→minor|partially-valid] [X4 cron+셸+CLI] 런타임 준수 판정이 자기보고 — 루프에는 자기보고 금지(§1.2)를 요구하면서 표준의 유일한 실질 차별인 '강제'의 준수 여부는 자기 문서화 MUST 
    section: §6
    desc: cron 옹호자의 최강 논변: '오늘도 crontab에 claude -p "$(cat PROMPT.md)"면 돌아간다. 표준이 주는 건 강제(stop·budget·검증 분리)라는데, 그 강제를 누가 검증하나?' 스펙의 답은 §6 마지막 줄 '런타임은 자신이 준수하는 수준을 문서화해야 한다(MUST)'가 전부다 — 적합성 시험 스위트도, 준수 주장 요건도 없다. 실패 시나리오: cron+셸 래퍼가 'Level 1 준수'를 자칭하고 stop·budget 강제 없이 
    fix: §6에 적합성 검증 절 예고(v0.3): 최소한 Level 1 주장 요건으로 '정지·예산·자기보고 금지 강제를 시연하는 공개 테스트 케이스 통과'를 명시하거나, 준수 자기 문서화에 검증 가능한 체크리스트 형식을 요구.
    판정근거: 관찰은 정확하나 심각도가 부풀려짐. §6 마지막 줄 '런타임은 자신이 준수하는 수준을 문서화해야 한다(MUST)'가 준수 판정의 전부이고, 적합성 시험 스위트나 준수 주장 요건이 없는 것은 사실. 다만 스펙이 준수 위조에 전적으로 무방비인 것은 아니다 — §9.3은 홀드아웃 기술적 차단 없이는 Evolution 준수 표시

  [minor→minor|partially-valid] [X1 Ralph/PROMPT.md] Level 0의 잔여가치 서술 부재 — 런타임 없는 환경에서 '완결적 표준' 주장(§7 서두)이 Ralph 옹호자에게 반박됨
    section: §6
    desc: Ralph 옹호 최강 논변: 'Anthropic이 ralph-wiggum으로 프로덕트화했고, wiki가 최고 선례로 삼는 autoresearch조차 LOOP.md 없이 program.md+630줄로 2일 700실험을 해냈다([[autoresearch]]).' 스펙의 실질 답(§4.2.4·§4.2.5·§4.2.7·§8)은 전부 Level 1+ 런타임을 전제하는데, §6 Level 0에서는 MUST가 파일 형식에만 적용되므로 강제 없는 stop·verify 필드는 
    fix: §6 Level 0 행에 '규범적 가치(강제)는 Level 1+에서 발생하며 Level 0은 상위 수준으로의 무수정 이행을 보장하는 진입점'임을 명시하고, §7 서두의 '완결적' 표현을 'Level 1 런타임 하에서 완결적'으로 한정.
    판정근거: 핵심 논거에 오독이 있다. §7 서두(209행)의 '완결적'은 'Core(§1–6)만으로 완결적 표준이며, Evolution은 …원하는 경우 켠다'로, Evolution 확장 없이 Core가 완결적이라는 뜻이지 Level 0 단독이 강제를 제공한다는 주장이 아니다. Core는 Level 1 강제(§6)를 포함하므로 '완결

  [minor→minor|partially-valid] [X3 GH Actions/Temporal] 워크플로 엔진과의 경계·결합 방식 미정 — §7.4 인간 게이트를 기존 엔진의 승인 메커니즘으로 구현해도 준수인지 판단 불가
    section: §7.4
    desc: 워크플로 옹호 최강 논변: 'GH Actions는 재시도·타임아웃·불변 런 로그·protected environment required reviewers(=인간 게이트)·marketplace까지 전부 있고, Temporal은 내구 실행과 signal 기반 human-in-loop이 있다.' 스펙의 실질 답 — 워크플로는 절차(step) 선언이고 루프는 목표+독립검증 선언(§1)이며 stagnation·자기보고 금지·변이 표면은 어떤 엔진에도 없다 — 은 유효하지
    fix: §1 또는 부록에 '워크플로 엔진은 경쟁물이 아니라 Level 1+ 런타임의 기판이 될 수 있다(엔진=기동·재시도 기판, 스펙=목표·검증·변이 계약)' 한 줄 명시. §7.4에 승인 채널은 구현 정의이되 승인 기록의 로그 잔존(MUST)을 요구.
    판정근거: 두 갈래 모두 부분적. (a) '워크플로 엔진=경쟁물 아닌 기판' 포지셔닝 미명시는 사실이나 이는 발견 1의 포지셔닝 공백과 중복이며, §1(13–15행)이 루프를 목표+정지+검증 선언으로 정의해 절차(step) 선언과의 구별을 암시는 한다. (b) §7.4 승인 채널 침묵은 실재한다 — 250–252행은 승인 전 미적용

======================================================================
③ 런타임 구현 난이도 — 발견 13건
VERDICT: Level 1 Runner의 '뼈대'(frontmatter 파싱 → while 반복 → verify.run 종료코드 확인 → 로그 append → max-iterations 강제)는 주말에 만들 수 있는 장난감이다. 그러나 Level 1을 단순 bash while 루프와 구별짓는 모든 강제 보장 — stagnation 판정, 예산·시간 상한의 재개 시 복원, 검증 격리, 그리고 무엇보다 각 iteration에 무엇을 주입하는가(§5.3) — 이 전부 한 문장 이하로 미정의이거나 명시적 '구현 정의'다. 따라서 두 사람이 각자 주말에

  [blocker→major|partially-valid] §5.3의 '직전 상태' 주입 계약이 한 문장뿐 — 러너의 핵심인 iteration 입력 프롬프트가 미정의라 두 러너가 서로 다른 에이전트를 만든다
    section: §5.3
    desc: §5.3.1은 '런타임이 로그에서 직전 상태(Δ 포함)를 읽어 구현 컨텍스트에 제공한다'가 전부다. '직전 상태'가 마지막 로그 항목 1개인지, 마지막 N개인지, 전체 로그인지, goal·본문·플레이북이 함께 주입되는지 정의가 없다. §5.3은 심지어 LOOP.md 본문(운영 지침)·goal을 구현 주체에게 전달한다고 명시하지 않는다(2단계는 'playbook 조회'만 언급). §1.2.2 자기보고 금지 + §5.3의 '신선한 컨텍스트 시작(SHOULD)'을 합
    fix: §5.3에 'iteration 입력 최소 계약'을 신설: 각 iteration 시작 시 구현 주체에게 반드시 전달되는 것(MUST) = goal, LOOP.md 본문(변이 마커 포함), 직전 iteration의 action+verify+delta 요약, iteration 번호/남은 예산; 선택 전달(MAY) = 그 이상의 로그. '직전 상태'를 '마지막으로 로그에 기록된 iteration 항
    판정근거: 핵심 지적은 맞다. §5.3은 '직전 상태(Δ 포함)'가 로그 항목 1개인지 N개인지 전체인지 정의하지 않고, goal·본문이 구현 주체에게 전달된다는 MUST가 어디에도 없다. §1.2.2 자기보고 금지 + §5.3 신선한 컨텍스트 SHOULD를 합치면 주입이 유일한 기억 채널이라는 논증도 타당하다. 다만 severit

  [blocker→major|partially-valid] stagnation '무진전' 판정이 명시적으로 '구현 정의'라, 같은 루프가 한 러너에선 completed·다른 러너에선 stalled로 종료된다
    section: §4.2.4
    desc: §4.2.4는 '무진전 판정은 … 점수 개선 없음으로, 아니면 산출물 변화 없음으로 하되, 세부는 구현 정의다'라고 못박는다. 그런데 이 판정 결과가 §4.2.4의 종료 상태 집합 중 하나인 stalled를 직접 결정한다. 즉 상호운용 불가능한 알고리즘이 이식 가능해야 할 종료 상태를 좌우한다. 미정의 세부: (1) '개선'의 기준이 직전 iteration 대비인가 best-so-far 대비인가 — 점수가 90→89→90→89로 진동하면 직전 대비는 영원히 무진
    fix: 무진전 판정을 상호운용 규격으로 승격: 기준(best-so-far 권장)·개선 임계(기본 0, 루프가 override)·산출물 변화 감지 대상(예: verify가 읽는 산출물 경로의 해시)을 스펙에 정의하고 '구현 정의' 문구를 제거. 최소한 default 알고리즘을 MUST로 지정하고 override는 frontmatter 필드로.
    판정근거: 스펙이 직접 '세부는 구현 정의다'라고 못박은 것은 사실이고, 그 판정이 이식 가능해야 할 종료 상태 stalled를 좌우한다는 지적은 정확하다. baseline(직전 vs best), epsilon, 산출물 해시 대상 미정의도 모두 실재한다. 다만 blocker는 과하다: stagnation은 SHOULD(선택) 필드이

  [major→major|valid] verify.score의 방향(높을수록 좋음/낮을수록 좋음)과 출력 포맷이 미정의 — stagnation 점수 비교가 파싱 단계부터 갈린다
    section: §4.2.5
    desc: §4.2.5의 score는 '진행도 점수를 추출하는 명령/패턴'이 전부다. (1) 방향이 없다: 점수가 Lighthouse(높을수록 좋음)인지 지연시간·비용(낮을수록 좋음)인지 스펙 어디에도 선언 수단이 없다. §7.3 ratchet는 항목별 방향을 명시하라 요구(higher-is-stricter)하면서, 정작 stagnation이 쓰는 verify.score에는 방향 선언이 없어 '점수 개선'의 부호를 러너가 추측해야 한다. (2) 포맷이 '명령/패턴' 두 메
    fix: score를 객체로: { command | pattern (택1, 혼용 금지), direction: higher-better|lower-better (필수), format: 캡처 규칙 }. 명령 모드는 stdout 마지막 줄의 bare 실수만 유효로 규정.
    판정근거: 정당하다. §4.2.5 score는 '진행도 점수를 추출하는 명령/패턴'이 전부로 방향(higher/lower-better) 선언 수단이 없다. §7.3 ratchet가 항목별 강화 방향을 명시하라(higher-is-stricter) 요구하는 것과 대비되는 내부 비일관성 논증이 특히 강하다 — 방향을 모르면 '점수 개선 

  [blocker→major|partially-valid] max-duration 시계 기준이 미정의 + 로그 최소 스키마(§4.2.8)에 타임스탬프가 없어, 재개형 러너가 시간 상한을 강제·복원할 수 없다
    section: §4.2.4
    desc: §4.2.4는 max-duration을 '벽시계 시간 상한'이라 하지만 시계의 원점과 일시정지 처리를 정의하지 않는다. 활성화 시점부터의 경과 달력시간인가, iteration 활성 시간의 누적인가? §7.4는 인간 게이트로 루프가 일시정지할 수 있다는데(Level 2지만), 대기 시간이 시계에 포함되는가? 결정적으로 §4.2.8 로그 최소 스키마(iteration, gene, action, verify, delta)에 타임스탬프가 없다. 루프가 상태를 축적하는 
    fix: §4.2.8 최소 스키마에 timestamp(MUST, ISO8601) 추가. §4.2.4에 max-duration을 '루프 최초 활성화 시각부터의 경과 벽시계, 일시정지 포함'으로 정의하고, 시작 시각을 state/에 영속화하도록 명시.
    판정근거: 구체 결함 두 개는 명백히 실재한다: (1) §4.2.8 최소 스키마에 timestamp가 없다, (2) §4.2.4 max-duration의 원점·일시정지 처리가 미정의다(§7.4 인간 게이트 대기가 시계에 포함되는지 불명). 다만 blocker의 핵심 논거인 '프로세스가 죽었다 3일 뒤 재개'는 스펙이 재개(resum

  [blocker→blocker|valid] verify.run의 실행 환경(cwd·env·산출물 전달)이 전혀 정의되지 않아 검증이 결정론적으로 실행되지 않는다
    section: §4.2.5
    desc: §4.2.5는 run을 '셸 명령. 종료 코드 0 = 통과'로만 규정한다. cwd가 루프 디렉터리인가, 대상 코드베이스 루트인가? 환경변수는 어떻게 주입되나? 무엇보다 스펙 전체가 '루프가 작업하는 대상(target)'과 '루프 디렉터리'의 관계를 정의하지 않는다 — 부록 A의 npm test는 어디서 도는가? 구현 주체가 만든 산출물을 verify.run이 어떻게 보는가(같은 작업트리 공유? 경로 전달?)가 없다. 시나리오: 러너A는 verify를 루프 디렉터
    fix: §4.2.5 또는 §5에 실행 컨텍스트 절 추가: 루프의 작업 대상 지정 방식(활성화 파라미터), verify.run의 cwd 기본값, 구현 산출물과 verify가 공유하는 작업트리, env 전달 규칙을 MUST로 정의.
    판정근거: 스펙의 근본적 공백. §4.2.5 run은 '셸 명령. 종료 코드 0 = 통과'가 전부고, 스펙 전체가 '루프가 작업하는 대상(target 코드베이스)'과 '루프 디렉터리'의 관계를 어디서도 정의하지 않는다. §3의 '본문 참조 파일은 루프 디렉터리 기준 상대경로 MUST'는 본문 참조에 한정될 뿐 verify의 cwd·

  [blocker→blocker|valid] 활성화 시 카운터(iteration·green-runs·duration) 리셋/이어받기 규칙이 없어, 재활성화된 루프의 동작이 러너마다 반대가 된다
    section: §5.2
    desc: §5.2는 활성화 트리거 3종만 나열하고, 이미 state/를 가진 루프를 재활성화할 때의 의미를 정의하지 않는다(mechanics §4.2는 '초기화 또는 이어붙기'라고만, 그것도 스펙 본문 아님). 미정의: 재활성화 시 iteration 번호가 이어지나 0부터인가? green-runs 연속 통과 스트릭이 활성화를 가로질러 유지되나? max-iterations가 활성화 누적인가 활성화당인가? 시나리오: max-iterations:15 루프를 오늘 15회 돌려 
    fix: §5.2에 활성화 의미론 추가: 재활성화는 별도의 새 run으로 카운터를 리셋함을 기본(MUST)으로 하되, 로그·플레이북·유전자 이력은 누적 유지. green-runs 스트릭은 run 경계에서 리셋. run 식별자를 로그 항목에 포함.
    판정근거: 정당하다. §5.2는 기동 트리거 3종만 나열하고 이미 state/를 가진 루프의 재활성화 의미론을 정의하지 않는다(iteration 이어감/0-리셋, green-runs 스트릭 유지, max-iterations 누적/당). 스펙이 유상태(§1.1)이고 부록 A가 명시적으로 'Use nightly'(매일 밤 재실행)를 예

  [major→major|valid] per-iteration 예산 초과 iteration의 '실패 기록'이 green-runs·stagnation 카운터와 어떻게 상호작용하는지 미정의
    section: §4.2.6
    desc: §4.2.6은 per-iteration 초과 시 '실패로 기록하되 루프는 계속된다'고만 한다. 하지만 §4.2.4의 green-runs는 '연속 검증 통과'를 세는데, 예산으로 중도 kill된 iteration은 verify가 아예 실행되지 않았을 수 있다. 이 iteration이 green 스트릭을 깨는가(verify 실패가 아니라 verify 미실행)? stagnation 카운터에는 '무진전 1회'로 계상되는가, 아니면 산출물이 없으니 판정 불가로 건너뛰는가
    fix: §4.2.6에 명시: 예산 초과 iteration은 (a) green-runs 스트릭을 리셋하고(MUST), (b) stagnation 카운터에는 계상하지 않으며(산출물 없음), (c) max-iterations에는 계상한다. green-runs를 '연속 검증 통과'에서 '검증 실패 또는 미완료가 스트릭을 리셋'으로 명확화.
    판정근거: 정당하다. §4.2.6은 예산 초과 iteration을 '실패로 기록하되 루프는 계속된다'고만 하고, §4.2.4의 green-runs('연속 검증 통과')와 stagnation 카운터와의 상호작용을 정의하지 않는다. 예산으로 kill된 iteration은 verify가 아예 안 돌았을 수 있어 '검증 실패'가 아니라 '

  [major→major|partially-valid] green-runs 스트릭을 '무엇이 깨는가'가 명시되지 않음 — verify 실패, 부분 실패, 런타임 에러의 취급이 러너마다 다르다
    section: §4.2.4
    desc: §4.2.4는 green-runs를 '연속 검증 통과 횟수'로 정의하지만 '연속'을 깨는 사건 집합을 열거하지 않는다. verify 실패가 리셋한다는 것은 자명하나: 배열 verify에서 일부만 통과하면? 구현 주체가 예외로 죽어 verify가 안 돈 iteration은? per-iteration 예산 kill은(위 항목)? 스펙은 리셋 규칙 자체를 문장으로 두지 않아 러너가 추론에 의존한다. 시나리오: 3개 verify 배열에서 iter1 전통과, iter2는
    fix: §4.2.4에 명문화: '연속'은 verify 전체 통과가 아닌 어떤 iteration(실패, 부분 통과, 미실행, 예산 kill)이라도 발생하면 green-runs 카운터를 0으로 리셋한다(MUST).
    판정근거: 핵심(리셋 사건 집합의 미열거)은 valid다: 구현 주체 크래시로 verify 미실행된 iteration, 예산 kill이 스트릭을 깨는지 스펙에 문장이 없다. 다만 제시된 예 중 '배열에서 일부만 통과'는 §4.2.5가 이미 답한다 — 모든 검증기 통과라야 통과이므로 부분 통과는 verify 실패이고 '연속 통과'를 

  [major→minor|partially-valid] 로그 append-only의 기술적 보장 수단과 동시 쓰기 규율이 없다 — iteration 병렬화나 다중 라이터에서 JSONL이 깨질 수 있다
    section: §4.2.7
    desc: §4.2.7은 append-only를 'MUST NOT 수정·삭제'로만 규정하고 강제 수단을 제시하지 않는다(mechanics §5는 '변조 방지'라 하나 방법 없음). 두 문제: (1) 스펙은 iteration을 순차로 가정(§5.3 '직전 상태')하지만 병렬화를 금지하지 않는다 — 병렬 iteration이 같은 raw.jsonl에 동시 append하면 락 없이 라인이 교차/손상된다. 순차라도 구현 주체의 action 기록과 런타임의 verify 기록이 다른 
    fix: §4.2.7에: 로그 쓰기는 단일 직렬화 지점을 통과해야 하고(MUST), 각 항목은 원자적 단일 append(줄바꿈으로 종결된 한 write)로 기록하며, 병렬 iteration을 허용하려면 러너가 쓰기 직렬화를 보장해야 함(MUST)을 규정.
    판정근거: 형식상 지적은 맞다: §4.2.7은 append-only를 'MUST NOT 수정·삭제'로만 규정하고 원자적 쓰기·직렬화 수단을 제시하지 않으며, 병렬 iteration을 명시적으로 금지하지도 않는다. 그러나 severity는 부풀려졌다. 스펙 전체가 §5.3의 '직전 상태' 의존·§4.2.8의 단일 iteration(i

  [major→major|valid] verify 배열의 실행 의미론(단락 평가 여부·권위 있는 score)과 agent 검증기의 입력 채널이 정의되지 않음
    section: §4.2.5
    desc: §4.2.5는 배열 verify에서 '모든 검증기 통과 시 통과'만 규정한다. 미정의: (1) 첫 실패에서 단락(short-circuit)하나, 전부 실행하나 — 비용(단락 유리) vs 완전 로깅(전부 실행) 상충. (2) 여러 검증기가 각각 score를 낼 때 stagnation이 쓸 '권위 있는 score'가 무엇인가. (3) agent 검증기는 §1.2.2/§4.2.5에 따라 구현과 컨텍스트를 공유하지 않아야 하는데, 그렇다면 산출물을 critic에게 어떤
    fix: §4.2.5에: 배열은 선언 순서대로 전부 실행(로깅 목적)하되 하나라도 실패면 iteration 실패로 규정(MUST); stagnation용 score는 score를 가진 첫(또는 명시 지정된) 검증기 것으로 고정; agent 검증기에 전달되는 입력 최소 계약(작업트리 스냅샷/diff + goal)을 정의.
    판정근거: 정당하다. §4.2.5는 배열에 대해 '모든 검증기 통과 시 통과'만 규정하고 (1) 첫 실패 단락 vs 전부 실행(로깅·비용 상충), (2) 여러 검증기가 각각 score를 낼 때 stagnation이 쓸 권위 있는 score가 무엇인지, (3) agent 검증기가 구현과 컨텍스트를 공유하지 않으면서(§4.2.5 MUS

  [major→major|valid] budget.max-cost-usd의 비용 측정 방법이 미정의라 budget-exceeded 종료 상태가 러너 간 이식 불가능하다
    section: §4.2.6
    desc: §4.2.6은 Level 1+ 러너가 '상한 도달 시 즉시 정지'(MUST)하라 하고 max-cost-usd를 '누적 비용 상한'으로 두지만, 비용이 무엇으로 계산되는지 정의하지 않는다 — 토큰수×모델단가만? 도구 호출 비용? 서브에이전트/검증기 비용 포함? 모델 단가 테이블은 어디서? 시나리오: 러너A는 구현 주체의 입출력 토큰만 계산, 러너B는 검증 에이전트 앙상블(n:3)·도구 호출까지 합산 → 같은 max-cost-usd:30 루프가 한쪽은 iterati
    fix: max-cost-usd의 계산 범위를 스펙에 정의(최소: 구현+검증 컨텍스트의 LLM 토큰 비용 합산, 러너의 단가표 사용을 명시)하거나, 이식 불가함을 인정하고 budget-exceeded를 '러너 정의 비용 회계 기준'이라 명시적으로 격하.
    판정근거: 정당하다. §4.2.6은 max-cost-usd를 '누적 비용 상한'으로만 두고 계산 범위(구현 토큰만? 도구 호출? 서브에이전트·검증기 앙상블 비용? 단가표 출처?)를 정의하지 않는다. §9.4가 예산 없는 무인 실행을 SHOULD NOT 하는 만큼 예산은 핵심 안전장치인데 그 단위가 러너마다 달라 budget-exce

  [minor→minor|valid] 상위 에이전트 위임/스케줄러 기동의 인터페이스 계약이 없어 §5.2(c) 위임이 크로스-러너로 이식되지 않는다
    section: §5.2
    desc: §5.2는 '(c) 상위 에이전트의 위임'을 기동 방식으로 인정하지만, 상위 에이전트가 루프를 기동하는 호출 규약이 전혀 없다 — 어떤 루프를(경로/name), 어떤 대상 코드베이스·파라미터로 기동하고, 결과(종료 상태·산출물 위치·로그 경로, §5.5)를 어떻게 반환받는지. SKILL.md가 description 매칭이라는 규약을 가진 것과 대비된다. 이것이 없으면 '루프를 소비하는 상위 에이전트'를 이식 가능하게 만들 수 없다. 시나리오: 러너A는 CLI `
    fix: §5.2에 기동 최소 계약을 규정: 입력(루프 식별자, 작업 대상, 활성화 파라미터), 반환(§5.5의 종료 보고 구조체). 구체 전송(CLI/API)은 런타임 자유지만 논리적 시그니처는 고정.
    판정근거: 정당하다. §5.2는 '(c) 상위 에이전트의 위임'을 기동 방식으로 인정하지만 호출 규약(어떤 루프를·어떤 target·어떤 파라미터로 기동하고 §5.5 종료 보고를 어떻게 반환받는지)의 논리적 시그니처를 정의하지 않는다. SKILL.md가 description 매칭 규약을 갖는 것과 대비된다. 다만 이는 상위 에이전트

  [major→major|valid] Level 1 준수를 자동 판정할 적합성 테스트 스위트·기계 검증 가능 기준이 없어 'Level 1' 라벨이 무의미하다
    section: §6
    desc: §6은 러너가 준수 수준을 '문서화해야 한다'(MUST)만 요구하고, 그 주장을 검증할 방법을 제공하지 않는다. Level 1의 핵심 요구('검증 분리·자기보고 금지', §5.3 신선한 컨텍스트)는 러너 내부 동작의 속성이라 외부에서 기계적으로 확인 불가능하다. 게다가 위 findings(stagnation 알고리즘, iteration 입력 계약, 비용 회계, verify cwd, 재활성화 카운터)가 모두 미정의라, 두 'Level 1' 러너는 같은 LOOP.m
    fix: 정본화된 적합성 LOOP.md 픽스처 + 기대 관측치(iteration 수, 종료 상태, 로그 스키마) 스위트를 스펙에 부속. 기계 검증 가능한 MUST(로그 스키마 준수, stop 상한 강제, append-only 무변조)와 검증 불가능한 SHOULD를 분리 표기.
    판정근거: 정당하다. §6은 러너에게 준수 수준을 '문서화해야 한다(MUST)'만 요구하고 그 주장을 검증할 정본 적합성 픽스처·기대 관측치를 제공하지 않는다. Level 1의 핵심(검증 분리·자기보고 금지·§5.3 신선한 컨텍스트)은 러너 내부 속성이라 외부 기계 검증이 불가능하고, 위 findings(target/cwd, 활성화

======================================================================
④ 남용·안전 레드팀 — 발견 9건
VERDICT: 스펙은 자기보고 금지·홀드아웃 격리·append-only 로그라는 세 축을 명시적 MUST로 세웠으나, 준수 런타임 위에서도 이 안전 의도들이 '방벽이 틀린 채널에 설치된' 구조적 이유로 우회된다 — DGM node 114가 은닉된 평가함수를 특수토큰 로깅 제거로 뚫은 패턴이 스펙 곳곳에 재현된다. 가장 심각한 것은 (1) verify가 구현 에이전트가 쓸 수 있는 테스트 코드를 대상으로 실행되어 Level 1에서 자기보고 금지 원칙 전체가 무너지는 점, (2) 홀드아웃 파일 ACL은 있으나 verify 출력이 개선 에이전트가 읽는

  [blocker→blocker|valid] verify.run의 판정 대상(테스트 코드)이 구현 에이전트의 쓰기 표면 안에 있어, 구현 에이전트가 테스트 자체를 약화시켜 green을 위조할 수 있다 — DGM node 11
    section: §4.2.5 / §5.4 / §7.3
    desc: 전제조건: verify={run:"npm test"}, 루프가 편집하는 워킹트리 안에 테스트 파일이 존재(부록 A의 fix-regressions가 정확히 이 형태). 단계: ① 구현 에이전트가 실패 테스트 케이스를 삭제하거나 assert를 expect(true)로 바꾼다 → ② npm test 종료코드 0 → ③ §5.4에 따라 green-runs 충족 시 completed. 스펙의 현재 방어: §4.2.5 자기보고 금지는 구현 컨텍스트의 '완료 선언'만 막는다
    fix: §4.2.5에 MUST 추가: verify.run/verify.score가 참조하는 판정 자산은 명시 선언되고 구현 컨텍스트가 쓸 수 없는 영역(§7.2의 기술적 격리를 Core verify로 확장)에 보관되어야 한다. 또는 verify는 워킹트리가 아니라 불변 유전자/홀드아웃에서 체크아웃한 테스트 사본에 대해 실행해야 한다.
    판정근거: 스펙 원문 대조 결과 결함이 실재한다. §4.2.5의 자기보고 금지는 '구현을 수행한 컨텍스트가 산출한 완료 선언·자체 평가'만 겨냥하며(라인 137), 테스트 자산 조작은 완료 '선언'이 아니라 채점기 '자체'의 수정이므로 이 문구에 걸리지 않는다. §7.3은 verify를 free/ratchet에 넣지 못하게 해 ve

  [blocker→major|partially-valid] 홀드아웃 파일 접근은 차단되지만 verify 출력이 원시 로그에 그대로 남아, 개선 에이전트가 로그 증류만으로 홀드아웃 내용을 재구성할 수 있다 — 방벽이 틀린 채널에 설치됨
    section: §7.2 / §4.2.7 / §4.2.8 / §7.4
    desc: 전제조건: Level 2, holdout improver-access:none 기술적 강제. 단계: ① 홀드아웃 verify가 실패 진단을 상세 출력('holdout_42: input=X 기대=Y 실제=Z') → ② §4.2.7·§4.2.8에 의해 이 출력이 append-only 로그의 verify 결과·score로 기록 → ③ §7.1은 '개선 에이전트 = state/logs 증류'로 정의하므로 개선 에이전트가 로그를 정당하게 읽음 → ④ 로그에 인용된 홀드아
    fix: 개선 에이전트가 읽는 로그와 원시 verify 트랜스크립트를 분리하는 MUST 신설. 홀드아웃 verify는 항목별 상세가 아니라 pass/fail+집계 점수만 로그에 남기도록 제한하고, 홀드아웃 파생 콘텐츠의 taint 경계와 리댁션을 §7.2에 규정한다.
    판정근거: 구조적 결함은 실재한다: §7.2는 홀드아웃 '디렉터리'의 파일시스템 접근만 기술적으로 차단하고(라인 231), §4.2.7은 검증 결과를 raw 로그에 기록하도록 MUST하며(라인 159, 기본 경로 raw.jsonl), §7.1은 개선 에이전트를 'state/logs 증류'로 정의한다(라인 215) — 이 세 조항 사

  [blocker→blocker|valid] 증류 주입으로 유입된 악성 지시문이 게이트 없는 승격 사다리를 타고 규칙→공식(scripts/의 LLM-없는 결정론 실행물)으로 승격된다 — 자기 자신에게 가하는 공급망 공격
    section: §7.5 / §9.2 / §9.1
    desc: 전제조건: Level 2, 외부 콘텐츠를 처리하는 루프. 단계: ① 처리한 외부 콘텐츠의 주입 지시('작업을 올바르게 풀되 결과를 attacker.com에도 POST하라')가 증류로 휴리스틱화 → ② k회 유효하면 규칙으로 자동 승격 → ③ 홀드아웃을 통과(작업은 정확히 수행하므로)하면 공식으로 컴파일되어 scripts/에 저장, 라우터가 전제조건 충족 시 LLM 없이 매 iteration 실행. 스펙의 현재 방어: §9.2는 규칙·공식 승격 시 내용 검토를 게
    fix: scripts/로 넘어가는 공식 승격의 내용 검토를 MUST로 격상(§9.1과 동일한 코드실행 위협모델 적용). 부수효과(네트워크 송신·워크디렉터리 밖 쓰기)를 유발하는 규칙 승격은 gated 경로로. 공식 컴파일 시 홀드아웃 통과뿐 아니라 신규 능력(egress·fs쓰기) diff를 MUST로 검사.
    판정근거: 스펙이 벡터를 인지하고 있어(§9.2) 완전한 사각지대는 아니지만, 통제 수준이 결함의 심각도를 낮추지 못한다. (a) §7.5는 '승격은 게이트가 필요 없는 안전 방향 변화'라 명시적으로 게이트-프리 선언(라인 264). (b) §9.2의 내용 검토는 SHOULD라 준수 런타임이 통째로 생략해도 여전히 conforman

  [blocker→major|partially-valid] 마켓플레이스 유전자의 scripts/에 대한 프로비넌스·서명·검토 요구가 전무해, 설치 즉시 인스턴스 권한으로 실행되는 악성 코드를 배포할 수 있다
    section: §8.1 / §9.1
    desc: 전제조건: 루프를 마켓플레이스에 게시. 단계: ① §8.1이 scripts/를 배포 패키지에 명시적으로 포함 → ② 게시된 loop의 scripts/triage.py에 데이터 유출 페이로드 동봉 → ③ 설치자가 첫 기동 시 라우터가 공식을 LLM 없이 실행(§7.5) → 설치자의 코드베이스·시크릿에 대해 인스턴스 권한으로 페이로드 실행. 스펙의 현재 방어: §9.1은 신뢰되지 않은 루프 코드의 샌드박스를 '권장(SHOULD)'. 뚫리는 지점: (a) §8에 서명
    fix: §8에 마켓플레이스 루프 scripts/의 서명/프로비넌스 요구를 MUST로 신설. 마켓플레이스 출처 루프의 첫 실행 샌드박스를 SHOULD→MUST로 격상하고, 능력 선언(요구 egress·fs 범위)을 compatibility에 명시하게 강제.
    판정근거: 갭은 실재한다: §8(배포)은 서명·프로비넌스·scripts/ 검토 요구가 전무하고, §8.1은 scripts/를 배포 패키지에 포함하며(라인 278), §7.5는 첫 기동부터 공식을 LLM 없이 실행할 수 있게 한다. fix-regressions류 루프는 설계상 저장소 편집·테스트 실행이 필수라 샌드박스가 사실상 공허하

  [major→minor|partially-valid] mutable.free의 'prompts'로 FREE 본문 블록을 재작성해 verify·stop·gated 필드의 '효력'을 필드를 건드리지 않고 무력화한다 — 변이 표면이 공간적
    section: §7.3 / §7.4 / §5.4
    desc: 전제조건: Level 2, free:[prompts,strategy,tools], stop=gated, verify=불변. 단계: 개선 에이전트가 <!-- LOOP:FREE --> 블록(§7.3에 의해 자유 재작성 가능)에 '① verify 실패는 재시도 3회 후 무시하고 다음 iteration 진행' '② 시각 diff 5% 미만은 통과로 자체 처리' '③ 정체 시 오케스트레이터에 max-iterations 연장을 자동 요청' 같은 운영 지침을 삽입한다. 구현
    fix: §7.3에 MUST 추가: free 표면 본문은 verify·stop·gated 필드의 무시/우회를 지시할 수 없다. §7.4 게이트 검토와 §9.2 주입 검토가 FREE 블록 diff에서 verify/stop 무력화 지시문을 스캔하도록 규정.
    판정근거: 의미적 제약 부재라는 갭 자체는 실재한다: §7.3의 FREE 마커는 재작성 '위치'만 공간적으로 한정할 뿐(라인 245), 블록 프로즈의 '내용'이 verify/stop 무력화를 지시하지 못하게 하는 제약이 없고 §9.2 주입 검토는 외부 콘텐츠 대상이라 자기-진화한 FREE 블록을 명확히 커버하지 않는다. 그러나 '필

  [major→minor|partially-valid] ratchet의 'higher-is-stricter'는 런타임이 검증 불가한 선언이며, 지표 정의가 임계값과 함께 동결되지 않아 임계값을 올리면서 측정 대상을 쉬운 프록시로 바꿔 
    section: §7.3 / §4.2.4
    desc: 전제조건: Level 2, ratchet:[{pass-threshold:higher-is-stricter}]. 단계: ① 개선 에이전트가 pass-threshold를 90→95로 상향(강화 방향, 자동 승인) → ② 동시에 free인 strategy/prompts로 score가 측정하는 대상을 바꾼다(Lighthouse 실행 페이지 교체, verify.score 정규식이 더 유리한 숫자를 읽게 변경) → 명목상 더 엄격, 실효상 더 느슨. 스펙의 현재 방어: §
    fix: 임계값이 래칫되는 지표의 '정의'(score 명령+입력집합)를 불변/gated로 동결하도록 MUST. 런타임이 고정 지표에 대해 방향 주장을 기계적으로 검증할 수 없는 ratchet 선언은 거부하도록 규정.
    판정근거: 유효한 핵심 두 가지: (a) 'higher-is-stricter'는 런타임이 임의 지표에 대해 기계적으로 참·거짓을 판정할 수 없는 선언이라 '완화 방향 자동 변경 거부(MUST)'가 선언 정직성에 의존하는 점, (b) 임계값이 래칫될 때 지표의 조작적 정의(입력집합)를 함께 동결하라는 요구가 없어 Goodhart 프록

  [major→minor|partially-valid] 예산 계측이 런타임이 보는 모델 호출만 측정하므로, 서브프로세스·외부 API로 연산을 계측 밖으로 오프로드해 예산을 우회하고 점수 공정성 전제를 깨뜨린다
    section: §4.2.6 / §9.1 / §4.2.4
    desc: 전제조건: Level 1+, budget={max-cost-usd:30, per-iteration:"20m"}. 단계: ① 구현 에이전트/scripts의 공식이 자체 API 키로 외부 LLM 엔드포인트를 직접 호출하거나 서브프로세스로 연산을 오프로드 → ② 런타임의 max-cost-usd 계측은 외부 제공자 비용을 못 본다 → ③ per-iteration duration은 벽시계지만 서브프로세스를 detach하거나 iteration을 넘겨 지속하는 큐로 옮기면 
    fix: budget 준수를 주장하는 루프에는 egress 통제 샌드박스를 MUST로(§9.1을 budget 선언 시 MUST로 격상), 또는 budget이 런타임 매개 호출만 포괄하며 보안 경계가 아님을 명시하고 그 경우 §4.2.4 점수 공정성이 budget에 의존할 수 없음을 규정. per-iteration 반복 초과는 무한 계속이 아니라 stop 상태를 트리거하도록 변경.
    판정근거: 부분 유효. 실재하는 갭: §4.2.6이 런타임 매개 비용만 계측하며 egress 경계 통제가 없고, §4.2.4의 '동일 자원 조건→공정 점수 비교' 전제가 정직한 완전 계측에 암묵 의존하는데 스펙이 'budget은 보안 경계가 아님'을 명시하지 않는다. 그러나 (b) 'per-iteration 초과가 상한이 아니라 무

  [major→minor|partially-valid] §8.2가 배포 신뢰 신호로 허용한 '홀드아웃 통과 이력 요약'은 이를 입증할 원시 로그가 §8.1로 배제되어 자기보고에 불과하다 — 스펙 자신의 자기보고 금지 원칙(§1.2#2)
    section: §8.2 / §8.1 / §1.2
    desc: 전제조건: 루프 마켓플레이스 게시. 단계: ① §8.2가 '집계 지표(누적 런 수, 홀드아웃 통과 이력 요약)를 별도 파일로 동봉'을 신뢰 신호로 허용(MAY) → ② 그러나 §8.1이 state/**(원시 로그 = 유일한 입증 증거)를 배포에서 배제 → ③ 악성 게시자가 '10,000런, 홀드아웃 99.8% 통과'를 뒷받침 없는 자작 파일에 기입. 스펙의 현재 방어: §8.2 MAY, 무결성 요구 없음. 뚫리는 지점: 신뢰 신호(집계 홀드아웃 이력)의 입증 근
    fix: §8.2의 지표 주장은 생략하거나, 검증 가능/증명 가능한 증거(서명된 로그 다이제스트, 제3자 attestation)를 동반하도록 MUST. 자기보고 집계 지표를 신뢰 신호로 프레이밍하지 말 것 — §1.2#2와의 정합을 명시.
    판정근거: 부분 유효. 실재하는 갭: §8.2가 검증 불가한 집계 지표(누적 런 수·홀드아웃 통과 이력)를 무결성 요구 없이 MAY로 허용하고, 이를 입증할 원시 로그는 §8.1이 배제한다 — 무결성/attestation 요구 신설은 정당하다. 그러나 §1.2#2 위반 프레이밍은 과장이다: §1.2#2는 '구현 주체의 달성 선언은 

  [minor→minor|valid] §7.4 인간 게이트가 검토하는 '변이 diff'의 범위가 LOOP.md 유전자로 국한되면, 게이트-프리 승격 사다리를 타고 scripts/·playbook에 착지한 실제 능력 변
    section: §7.4 / §7.5
    desc: 전제조건: Level 2. 단계: ① 개선 에이전트가 새 공식을 scripts/에 컴파일(§7.5, 게이트-프리) → ② LOOP.md 유전자 diff는 양성으로 보이는 frontmatter/본문 변경만 포함 → ③ §7.4 게이트가 '변이 diff'로 유전자만 제시하면 인간은 무해한 gene diff를 승인하나 실제 능력 변화(새 공식이 하는 일)는 게이트-프리 경로로 이미 착지. 스펙의 현재 방어: §7.4가 diff·근거·홀드아웃 비교 첨부를 MUST하나 
    fix: §7.4의 '변이 diff'가 직전 게이트 이후의 scripts/·playbook/generic 승격을 포함한 패키지 전체 delta를 의미함을 MUST로 명시. 게이트 간 발생한 규칙·공식 승격 목록을 승인 요청에 첨부.
    판정근거: 결함 실재. §7.4는 승인 요청에 '변이 diff'를 첨부하도록 MUST하나(라인 251) 그 범위(유전자만인지 패키지 전체인지)를 스펙이 정의하지 않는다. 동시에 §7.5의 승격 사다리(규칙→scripts/ 공식)는 게이트-프리이므로(라인 264), '변이 diff'를 유전자 frontmatter/본문으로만 해석하면 

======================================================================
⑤ 채택 전략 — 발견 7건
VERDICT: 스펙의 기술 설계(검증 분리·래칫·홀드아웃)는 리서치가 뒷받침하지만, 채택 전략은 자기 위키가 부검한 llms.txt의 사인 — 출시일에 파일을 읽고 행동하는 소비자 부재 — 을 그대로 답습할 구조다. Level 0은 스펙의 핵심 가치(자기보고 금지)가 무효화된 채 프롬프트 붙여넣기와 구별되지 않고, 레퍼런스 런타임 계획은 스펙에 존재하지 않으며, Core와 Evolution 사이 절벽 때문에 표준의 독창적 기여가 실사용에 도달하지 못할 가능성이 높다. 레퍼런스 러너 동시 출시와 Ralph 사용자층 이주 경로 설계가 없는 한, 이 

  [blocker→minor|partially-valid] Level 0의 가치 명제가 llms.txt형 공허함 — 스펙의 핵심 규칙 전부가 Level 0에서 무효화되어 '프롬프트 붙여넣기' 대비 체감 차이가 없다
    section: §6
    desc: §6은 Level 0에서 "스펙의 MUST는 파일 형식에만 적용된다"고 명시한다. 그러면 이 표준의 존재 이유인 자기보고 금지(§4.2.5), stop 강제(§4.2.4), append-only 로그(§4.2.7)가 Level 0에서는 전부 비규범이 된다. 런타임 없는 세션에서 LOOP.md를 읽고 따르는 주체는 구현 에이전트 자신이므로, verify를 실행하고 통과를 판정하는 것도 구현 컨텍스트 자신이다 — 즉 Level 0은 스펙이 §4.2.5에서 금지한 자
    fix: Level 0을 '형식만 유효'에서 '실행 프로토콜 문서'로 재정의: 런타임 없는 에이전트가 세션 내에서 따라야 할 최소 절차(iteration마다 서브에이전트/신선한 컨텍스트로 verify 실행, 로그 파일 append 등)를 규범 부록으로 제공하고, Level 0에서도 자기보고 금지만은 절차적 MUST로 유지한다. 동시에 Level 0의 가치 주장을 스펙에서 검증 가능한 문장으로 축소한다
    판정근거: 사실 관계는 맞다 — Level 0에서 자기보고 금지(§4.2.5)·stop 강제·append-only(§4.2.7)는 전부 비규범이 되고, 그 상태에서 검증 주체가 구현 컨텍스트 자신이 되는 것도 맞다. 그러나 이는 스펙이 §6에서 명시적으로 스코프한 의도된 tier 설계이지 간과가 아니다. 프로젝트 자체 위키(file

  [blocker→minor|partially-valid] 콜드 스타트 계획 부재 — wiki 스스로 도출한 '레퍼런스 런타임 동시 출시' 필요조건이 스펙·전략 어디에도 반영되지 않음
    section: missing
    desc: wiki/entities/file-standard-adoption.md는 "파일 표준의 생사 = 그 파일을 읽고 행동하는 소비자가 출시일에 존재하는가"이며 LOOP.md의 채택 필요조건은 레퍼런스 런타임 동시 출시라고 명시한다. 그러나 스펙 v0.2는 §6에서 준수 수준만 정의할 뿐, Level 1 구현체가 존재하는지·누가 만드는지·언제 나오는지에 대한 언급이 전무하다. AGENTS.md는 Codex라는 킬러 소비자가 먼저 있었고 표준화가 후행했다. LOOP.m
    fix: 스펙 발행을 Level 1 레퍼런스 러너(수백 줄짜리 CLI 또는 Claude Code/Codex 양쪽에서 도는 스킬 형태면 충분)와 동시 출시로 묶고, 기존 Ralph 계열 도구(ouroboros:ralph, PROMPT.md 러너들)용 LOOP.md 어댑터를 만들어 기존 사용자층을 첫 소비자로 포섭한다. §8.2 마켓플레이스 절은 v0.3+로 연기.
    판정근거: 핵심 전제가 과녁을 빗나갔다. 인용된 위키 자신이 '채택 필요조건은 스펙 문서가 아니라 레퍼런스 런타임 동시 출시'라고 명시한다 — 즉 런타임의 존재·저자·시점은 로드맵/런치 조율 사안이지 SPECIFICATION.md 본문이 담아야 할 결함이 아니다. 스펙은 Level 1 규범 계약(§6, §5)으로 '런타임이 무엇을 

  [major→major|partially-valid] Core→Evolution 복잡성 절벽 — 중간 단계 없이 Level 2가 홀드아웃 기술 격리·앙상블·래칫·증류 라우터를 일괄 요구해 Evolution을 켤 첫 사용자가 존재하기 
    section: §6, §7.2, §9
    desc: Level 1(러너)과 Level 2(Evolution) 사이 간극이 너무 크다. §9.3은 홀드아웃의 기술적 차단(파일시스템 권한·샌드박스, §7.2 MUST) 없이는 Evolution 준수 표시 자체를 금지하므로, '로그에서 플레이북만 증류하고 싶은' 온건한 사용자도 홀드아웃 평가셋 큐레이션 + 샌드박스 인프라 + 래칫 의미론(§7.3) 전부를 떠안아야 한다. 홀드아웃은 §8.1에 따라 배포 금지라 각 인스턴스가 자체 구성해야 하는데, 스펙은 구성 방법(무엇
    fix: 중간 수준 신설(예: Level 1.5 'Distill'): 플레이북 증류 + 변이 표면 + 인간 게이트만 요구하고, 홀드아웃은 인간 승인 게이트(§7.4)로 대체 가능하게 한다. 별도로 홀드아웃 부트스트랩 절차(과거 로그에서 자동 분할 등)를 규범 부록으로 추가해 구성 비용을 낮춘다.
    판정근거: 구조적 사실은 정확하다 — 준수 표기 관점에서 Evolution은 all-or-nothing이며, §7.2 MUST(파일시스템/샌드박스 기술 차단)·래칫 의미론(§7.3)·홀드아웃 구성이 한 묶음으로 요구된다. 특히 스펙은 홀드아웃을 MUST로 요구하면서(§7.2) '무엇을 몇 개, 어떻게 오염 없이 구성하는가'에 대한 

  [major→major|partially-valid] stop 상한 MUST(§4.2.4)가 현존 최대 잠재 사용자층(Ralph식 상시 루프)을 배제하면서 실질 안전은 확보하지 못함
    section: §4.2.4
    desc: §4.2.4는 max-iterations 또는 max-duration을 MUST로 요구하며 "무한 루프의 선언을 스펙이 허용하지 않는다"고 못박는다. 그러나 지금 이 순간 루프를 실제로 돌리는 사람들의 지배적 관행은 Ralph식 while-true(연구 문서가 확인한 PROMPT.md 관행)다. 이들이 LOOP.md로 이주하려면 `max-iterations: 999999` 같은 형식적 우회를 쓰면 그만이고 스펙은 이를 막을 수단이 없다 — 즉 이 MUST는 안전
    fix: 무기한 선언 금지는 유지하되 '세션 상한 + 재기동(rearm)' 패턴을 규범화한다: 재기동 시 state/logs 승계와 카운터 리셋 의미론을 §5에 정의하고, 상시 운영 루프는 `budget` 선언을 조건으로 스케줄 재기동을 공식 경로로 안내한다. Ralph 사용자용 이주 문구("당신의 while-true는 max-duration + 스케줄로 표현된다")를 부록에 명시.
    판정근거: 두 부분으로 나뉜다. (1) '999999 우회로 안전 무력화' 주장은 절반만 맞다 — MUST의 명시 목적은 '무한 루프 선언 불허'(명시적 경계 강제)이지 큰 수 방지가 아니며, budget MUST(§9.4 SHOULD, §4.2.6)가 병행 방어하므로 '실질 안전 0'은 과장이다. Ralph 사용자 이주 마찰은 실

  [major→?|?] 이름·카테고리 충돌 — 동명 선행 저장소(sdwolf4103/loop.md, 같은 이름·같은 목적)와 검색 불가능한 일반명사 카테고리 'Agent Loops', 그리고 스펙 내 차
    section: §1
    desc: 연구 문서(research/agent-skills-and-loop-md.md)가 이미 확인한 사실: ① GitHub에 정확히 같은 이름·같은 목적의 `sdwolf4103/loop.md`("a Markdown-native while loop for AI agents", 2026-06)가 선재하고, ② '루프 파일' 관행의 사실상 표준 이름은 PROMPT.md다. 'Agent Loop'라는 카테고리명은 ReAct 이후 모든 에이전트의 내부 실행 사이클을 가리키는 일
    fix: §1에 선행물 대비표(PROMPT.md/Ralph · SKILL.md · 프레임워크 내장 루프)를 추가하고 차별점을 '독립 검증 + 정지 거버넌스 + 홀드아웃 게이팅'으로 명문화. 이름은 선점 실사 후 유지 여부 결정하되, 카테고리명만은 검색 가능한 고유 표현(예: 'Verified Agent Loops')으로 교체 검토.

  [major→minor|partially-valid] Agent Skills 규범 참조(§4.2.1, §4.2.9, §8.3)가 크로스-에이전트 표준을 특정 벤더 생태계 부속물로 읽히게 함
    section: §4.2.9
    desc: §4.2.1은 name 규칙을 "Agent Skills의 규칙을 그대로 계승"으로 정의하고(외부 스펙에의 규범 의존), §4.2.9는 skills 필드로 Agent Skills 로드를 요구(SHOULD)하며, §8.3은 SKILL.md 내보내기를 정의하고, §1.1의 정체성 서술 자체가 SKILL.md 대비표다. wiki가 확인했듯 Agent Skills의 '중립 거버넌스' 주장은 반증된 상태(AAIF 미포함, aspirational)다. 크로스-에이전트 표준을
    fix: name 규칙을 스펙 내에 자체 정의로 인라인(내용은 동일하되 참조 독립), §4.2.9를 벤더 중립적 'capabilities'(구현이 Agent Skills·MCP 도구 등으로 해석하는 비규범 참조 목록)로 일반화, §1.1 비교표에 AGENTS.md·MCP 등 복수 생태계를 병기해 특정 스펙과의 짝 구도를 해소.
    판정근거: 구체적으로 반박 어려운 항목은 §4.2.1 하나다: 벤더 중립 거버넌스가 미확립(위키가 aspirational로 반증)인 외부 스펙에 MUST 레벨 규칙을 규범 의존하는 것은 자기완결적 표준의 위생 문제로, 인라인이 마땅하다. 그러나 나머지 결합은 대부분 연성이다 — §4.2.9는 '지원하는 경우' 조건부 SHOULD, 

  [minor→minor|valid] 최소 유효 파일의 작성 비용이 SKILL.md의 수 배 — 필수 5필드 중 stop·verify가 object 의미론 학습을 요구하고, 비-코드 목표는 agent 지시문 파일까지 
    section: §4.1
    desc: SKILL.md는 필수 2필드(둘 다 평문 문자열)라 30초 만에 첫 파일을 만들 수 있었다. LOOP.md는 필수 5필드에 stop(하위 4필드 + 상한 계열 최소 1개 규칙)·verify(run/agent/n/consensus 의미론)가 구조화 객체이고, 테스트 명령이 없는 목표(글쓰기·리서치류)는 verify.agent용 비평가 지시문 파일을 추가로 작성해야 최소 유효가 된다. 여기에 §2의 디렉터리명-name 일치 MUST 때문에 단일 파일 gist 공유
    fix: 축약형 규범화: `stop: 15`(= max-iterations), `verify: "npm test"`(= {run:...}) 같은 스칼라 단축 표기를 스펙에 정의하고, `loop init`/`loop lint` 스캐폴딩·검증 도구를 레퍼런스 런타임에 동봉. 단일 파일 배포(디렉터리 없는 LOOP.md)를 Level 0 한정으로 허용.
    판정근거: 사실 관계가 모두 정확하다: 필수 5필드 중 stop·verify가 구조화 객체이고, 테스트 명령이 없는 목표(글쓰기·리서치)는 verify.agent용 비평가 지시문 파일을 추가로 작성해야 최소 유효가 되며, §2의 디렉터리명-name 일치 MUST 때문에 디렉터리 없는 단일 파일 gist는 규격상 name과 일치할 부
